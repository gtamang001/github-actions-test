# this action is being called from another action 
name: CI 
<<<<<<< HEAD
on: pull_request
=======

on: workflow_dispatch

>>>>>>> main
jobs: 
  build-job: 
    runs-on: ubuntu-latest
    steps: 
      - name: checkout code and version set 
        run: |
          echo "Build stage is done "
      - name: create artifact and store it 
        run: |
          mkdir test 
          cd test 
          echo "This is the content of the text file " >> artifact.txt 
      - name: store artifact 
        uses: actions/upload-artifact@v2
        with: 
          name: artifact 
          path: |
            test/*


  call-cd: 
    name: call cd 
    uses: gtamang001/github-actions-test/.github/workflows/cd.yml@main

